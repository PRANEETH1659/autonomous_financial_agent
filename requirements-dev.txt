# Test-only dependencies. Not required to run the app itself - see
# requirements.txt for that. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.4
pytest-mock>=3.12
